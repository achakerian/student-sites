{
  "name": "student-sites-worker",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "fflate": "^0.8.2"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.30.0"
  }
}
